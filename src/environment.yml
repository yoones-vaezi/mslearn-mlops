channels:
  - defaults
dependencies:
  - python=3.8
  - pip:
    - mlflow==1.30.0
    - cloudpickle==2.2.0
    - scikit-learn==0.24.1
    - azureml-inference-server-http
    - numpy
name: diabetes-deploy-env
